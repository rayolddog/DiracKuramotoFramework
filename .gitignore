# Python
__pycache__/
*.py[cod]
*$py.class

# IDE
.idea/
.vscode/

# Claude Code
.claude/

# Notebooks
TestQiskit.ipynb

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# macOS
.DS_Store

# IDE (JetBrains Junie)
.junie/

# Working notes / external feedback
*.txt

# Partial bibliography collection (abandoned due to paywalls; kept locally)
/references/

# Local proposal/working files (drafts for review before paper edits, kept local)
*_PROPOSALS.md

# Untracked illustrative/working figures (the tracked ones stay tracked)
tests/bell_phase.png
tests/bulk_sync_hardware.png
tests/bulk_sync_hardware2.png
tests/dirac_extension.png
tests/everett_thermal.png
tests/higgs_clock.png
tests/kuramoto_sync.png

# WIP: Zitterbewegung-synchronization numerics, figures & session notes
# (moved here 2026-06-15; un-ignore once §2.5 / the open-system study is finalized)
ZBW_synchronization/

# Timestamped editing snapshots (*_backup_<stamp>.md / .sh) — kept LOCAL.
# These are checkpoints, not history: git already carries the history, and the repo is
# public. Added 2026-09-01; supersedes the narrower PAPER_REVISED_v2 line below, which is
# kept so existing clones go on ignoring it.
*_backup_*
PAPER_REVISED_v2_backup_*.md

# External submission correspondence + cover letter — kept LOCAL.
# Repo is public and the endorsement drafts name third parties.
/submission/
/COVER_LETTER.md
/COVER_LETTER.pdf

# Incoming external research bundle (zip + notes) — kept local
/Research/

# Session working notes / brainstorm scratch — kept LOCAL (repo is public)
/sessions/

# WIP: limiting-curvature / black-hole-interior desynchronization draft
# (added 2026-08-23; un-ignore once the section is finalized)
limiting_curvature/
