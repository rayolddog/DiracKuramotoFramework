# Raw provenance layer — Born-Selection review round

These are the original per-reviewer working folders from the 2026-07-26/27 panel (JB's local directories, added to the repository 2026-07-28), preserved beneath the published record they support. Before inclusion, every file was verified against the published round (checks run 2026-07-28; session record):

1. **Package integrity.** Each reviewer folder contained a copy of the manuscript PDF and the simulation suite as sent. All four PDF copies are hash-identical (MD5 `090849de1212c8eb00f777aae6e69153`) to the frozen `../reviewed_paper_v0.3_Born_Selection.pdf` — every reviewer provably judged the exact frozen text. All four `born_selection_sims/` copies are file-for-file identical to the repository's preserved original suite. These verified-redundant copies were removed rather than duplicated into history.
2. **Instrument integrity.** All four `REVIEWER_PROMPT.md` copies were byte-identical to one another — the round's "byte-identical instrument" claim is hash-verified, not merely asserted. One copy is archived as `../REVIEWER_PROMPT_as_sent.md` (the as-sent instrument was not separately archived when the round was first published; this closes that gap).
3. **Review fidelity.** The raw returned reviews match the published verbatim copies exactly: `Review_GPT5_Codex.md` and `review_fable5_2026-07-26.md` are byte-identical to their counterparts in `../reviews/`; the two `.odt` returns (Gemini, SuperGrok) and `AdditionalInteraction.odt` (the GPT-5 follow-up) extract (textutil, plain text) to output byte-identical to `../reviews/external_Gemini_1.5Pro_Google.md`, `../reviews/external_SuperGrok_xAI.md`, and `../reviews/GPT5_followup_energy_and_amplitudes.md` respectively. Nothing in the raw layer is unpublished; nothing published diverges from what was received.

Retained here: only the original returned artifacts in their as-received formats (.md, .odt). The `.odt` files are kept because they are the primary artifacts as saved from the reviewer sessions; the published `.md` files are their verified plain-text extractions.
